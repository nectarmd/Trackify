import { Monitor } from "lucide-react";
import { ComingSoon } from "@/components/layout/coming-soon";

export default function QuiosquesPage() {
  return (
    <ComingSoon
      icon={Monitor}
      title="Quiosques"
      description="Configure terminais compartilhados para que a equipe registre o ponto em um único dispositivo. Recurso em breve."
    />
  );
}
